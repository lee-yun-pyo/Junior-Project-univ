extends base.pug

block content
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(name="name" type="text" required:true placeholder="Name" value=loggedInUser.name)
        input(name="id" type="text" required:true placeholder="ID" value=loggedInUser.id)
        input(type="submit" value="수정")
    hr
    a(href="change-password") 비밀번호 변경 &rarr;